# Copy this file to .env (which is gitignored) and set values for your local environment.
# .env is never committed — each developer maintains their own copy.

# Port for the AI workflow's Storybook instance (used by /figma-example-story).
# Set this to a unique port per worktree so concurrent runs never collide.
STORYBOOK_AI_PORT=6100
